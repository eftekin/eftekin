<p align="left">
software engineer in istanbul, backend and platform focused. been on GitHub since {{ new Date(user.createdAt).getFullYear() }}, {{ computed.commits }} commits across {{ user.repositoriesContributedTo.totalCount }} repos. this week: {{ Math.floor(plugins.wakatime.time.total) }}h {{ Math.round((plugins.wakatime.time.total % 1) * 60) }}m coding, mostly {{ plugins.wakatime.languages.find(l => !['YAML', 'JSON', 'Markdown', 'Other', 'Text', 'Makefile'].includes(l.name)).name.toLowerCase() }}.
</p>
