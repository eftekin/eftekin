<p align="left">
software engineering student in istanbul, currently interning at peak. been on GitHub since {{ new Date(user.createdAt).getFullYear() }}, {{ computed.commits }} commits across {{ user.repositoriesContributedTo.totalCount }} repos. this week: {{ Math.floor(plugins.wakatime.time.total) }}h {{ Math.round((plugins.wakatime.time.total % 1) * 60) }}m coding, mostly {{ plugins.wakatime.languages[0].name.toLowerCase() }}.
</p>
